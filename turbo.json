{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": ["ANALYZE"]
    },
    "lint": {},
    "test": {},
    "dev": { "cache": false, "persistent": true }
  }
}